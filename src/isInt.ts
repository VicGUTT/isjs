import isInteger from './isInteger';

/**
 * Determines whether the given value is an integer.
 *
 * @alias isInteger This function is an alias of the `isInteger` function.
 *
 * @example
 * ```js
 * isInt(123.123); // false
 * isInt(123.0); // true
 * isInt(123); // true
 * ```
 */
const isInt = isInteger;

export default isInt;
